# docker-compose-linux.yml
# docker-compose --file docker-compose-linux.yml up
version: '3'
services:  
  cram:
    image: "arthurniedz/cram:headless-1.1"
    container_name: cram_container
    network_mode: host
    privileged: true
    entrypoint: ./lectures/init.sh
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - ./lectures:/home/lectures


